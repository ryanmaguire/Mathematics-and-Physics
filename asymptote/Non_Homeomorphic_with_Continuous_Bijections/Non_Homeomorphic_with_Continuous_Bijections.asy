// Import necessary stuff, set format to PDF file.
import graph;
import settings;
settings.outformat="pdf";
settings.render=4;

// Size of the output figure.
size(256);

// Color of the spaces.


draw((-5.0, 0.0)--(5.0, 0.0), dpen);